export default function PkgDebian() {
  return (
    <div className="page">
      <h2>Debian Package</h2>
      <p style={{ color: 'var(--text-muted)' }}>This deployment does not support Debian Package.</p>
    </div>
  )
}
