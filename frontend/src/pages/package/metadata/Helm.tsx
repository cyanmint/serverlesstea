export default function PkgMetaHelm() {
  return (
    <div className="page">
      <h2>Helm Metadata</h2>
      <p style={{ color: 'var(--text-muted)' }}>This deployment does not support Helm Chart.</p>
    </div>
  )
}
