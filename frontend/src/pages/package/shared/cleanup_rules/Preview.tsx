export default function CleanupRulesPreview() {
  return (
    <div className="page">
      <h2>Preview Cleanup Rules</h2>
      <p style={{ color: 'var(--text-muted)' }}>This deployment does not support Package Cleanup Rules.</p>
    </div>
  )
}
