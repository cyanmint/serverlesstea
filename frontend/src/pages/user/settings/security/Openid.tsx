export default function OpenidSettings() {
  return (
    <div className="page">
      <h2>OpenID Connect</h2>
      <p style={{ color: 'var(--text-muted)' }}>This deployment does not support OpenID.</p>
    </div>
  )
}
